void setup() {
  size(250, 250);
  background(150);
}
void draw() {
  // ellipse are drawn from the center
  ellipse(100, 100, 100, 100);
  // rectangles are drawn from the top left corner
  rect(100, 100, 100, 100);
}