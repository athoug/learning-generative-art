int[] numberLine;  // initialize an array

void setup() {
  numberLine = new int[100]; // define the new array and give it a size

  for (int i = 0; i < 100; i++) {
    numberLine[i] = i;
    println(numberLine[i]);
  }
}
void draw() {
  ;
}