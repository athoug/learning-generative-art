// the draw function is run 60 times per second 
// it's helpful to animate objects on the screen

// notice the opening and closing curly braces 
// everything between these braces are run many times per second
void draw() {
  background(241, 196, 15);
  line(random(100), random(100), random(100), random(100));
}